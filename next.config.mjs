/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['links.papareact.com', 'images.unsplash.com','randomuser.me'],
    },
  };
  
  export default nextConfig;
  